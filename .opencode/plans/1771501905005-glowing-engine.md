# Plan: Update AGENTS.md with Missing Important Information

## Gap Analysis

After thorough exploration, the current AGENTS.md is missing several critical pieces of information:

### Missing Sections

1. **Application Startup Flow** - Entry point and initialization sequence
2. **Thread Architecture** - Multi-threaded design with message passing
3. **Audio System** - WASAPI capture, synchronization, mixing
4. **Hotkey Implementation** - Windows registration and message handling
5. **Clip Saving Flow** - Detailed trigger-to-file process
6. **Two Capture Modes** - CPU readback vs Hardware pull (ddagrab)
7. **Software Encoder** - MJPEG implementation details
8. **Memory Limits & Constants** - Config constraints, channel sizes
9. **Channel Architecture** - Crossbeam vs tokio, buffer sizes

### Information to Add

#### 1. Application Architecture (new section)
- Main entry point: `src/main.rs`
- Startup sequence (logger → config → state → platform thread → capture)
- Timer resolution guard pattern
- Event loop with `tokio::select!`

#### 2. Thread Architecture (new section)
- Main thread (tokio runtime)
- Platform thread (hidden HWND, hotkeys, tray)
- Capture thread (DXGI)
- Encoder thread (FFmpeg wrapper)
- Audio capture threads (WASAPI system/mic)
- Clip saver task (spawn_blocking)
- Event bridge thread (crossbeam → tokio)

#### 3. Channel Architecture (new section)
- Channel types (bounded/unbounded crossbeam, tokio mpsc)
- Buffer sizes (frames: 32, packets: 64, metadata: 32)
- Crossbeam → Tokio bridge pattern

#### 4. Audio System (new section)
- WASAPI loopback for system audio
- WASAPI capture for microphone
- PCM format: 48kHz, 16-bit, stereo
- Timestamp synchronization via QPC
- Audio mixing in muxer (amix filter)

#### 5. Hotkey System (new section)
- Windows `RegisterHotKey()` API
- Hidden HWND for WM_HOTKEY messages
- Parsing hotkey strings ("Alt+F9" → modifiers + VK)
- Hotkey IDs and error handling

#### 6. Clip Saving Flow (expand existing)
- Trigger flow from hotkey to file
- Keyframe requirement and retry logic (5 retries, 150ms)
- SPS/PPS caching for stream validity
- Two muxing paths (H.264 copy vs MJPEG transcode)
- spawn_blocking for async compatibility

#### 7. Two Capture Modes (new section)
- CPU readback mode: DXGI → CPU BGRA → FFmpeg stdin
- Hardware pull mode: FFmpeg ddagrab filter (direct GPU)
- Controlled by `use_cpu_readback` config

#### 8. Software Encoder (new section)
- MJPEG format, quality 70
- Thread pool 2-8 workers
- BGRA → RGB → JPEG conversion
- Every frame is a keyframe

#### 9. Memory & Constants (new section)
- Config constraints table (framerate 1-240, bitrate 1-500, memory 512-16384 MB)
- FFmpeg timeouts (process: 10s, threads: 5s)
- Buffer sizes (stdin: 16 frames, channels: 32-64)

#### 10. Key Windows APIs (expand existing)
- DXGI Desktop Duplication
- WASAPI (loopback, capture)
- Win32 hotkeys and window creation
- System tray (tray-icon crate)
- QPC timing
- COM initialization
- Thread priority

## Implementation Plan

### Files to Modify
- `C:\coding\liteclip-recorder\AGENTS.md`

### Changes
1. Add **Application Architecture** section after Project Context
2. Add **Thread Architecture** section with diagram
3. Add **Channel Architecture** section
4. Add **Audio System** section
5. Add **Hotkey System** section
6. Expand **Clip Saving Flow** under Critical Code Patterns
7. Add **Two Capture Modes** section
8. Add **Software Encoder** section
9. Add **Memory & Constants** section
10. Expand **Key Windows APIs** section

### Verification
- Review updated AGENTS.md for completeness
- Ensure no duplication with existing content
- Verify all file paths and function names are accurate
